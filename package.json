{
  "name": "tooltip-ai",
  "version": "1.0.2",
  "private": true,
  "description": "ToolTip AI landing/demo. Production desktop app is the WPF host in src-dotnet (Microsoft Store MSIX).",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:wpf": "dotnet publish src-dotnet/TeachMeAI.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o ./dist"
  },
  "keywords": [
    "tooltip-ai",
    "windows",
    "screen-inspector"
  ],
  "type": "commonjs",
  "devDependencies": {
    "electron": "^44.2.0"
  }
}
