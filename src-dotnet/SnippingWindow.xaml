<Window x:Class="TeachMeAI.SnippingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TeachMe AI Snipping Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#22000000"
        Topmost="True"
        ShowInTaskbar="False"
        Cursor="Cross"
        Loaded="Window_Loaded"
        KeyDown="Window_KeyDown"
        MouseDown="Window_MouseDown"
        MouseMove="Window_MouseMove"
        MouseUp="Window_MouseUp">
    <Grid>
        <!-- Canvas for Selection Rectangle -->
        <Canvas x:Name="OverlayCanvas">
            <!-- Neon Selection Border -->
            <Rectangle x:Name="SelectionRect"
                       Stroke="#00F5A0"
                       StrokeThickness="2"
                       StrokeDashArray="4 2"
                       Fill="#1A00F5A0"
                       Visibility="Collapsed" />
            
            <!-- Dimension Tag -->
            <Border x:Name="DimensionTag"
                    Background="#CC0A0E19"
                    BorderBrush="#00F5A0"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="6 2"
                    Visibility="Collapsed">
                <TextBlock x:Name="DimensionText"
                           Foreground="#FFFFFF"
                           FontFamily="Consolas"
                           FontSize="11"
                           Text="0 x 0 px" />
            </Border>
        </Canvas>

        <!-- Top Instruction Banner -->
        <Border HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Margin="0 24 0 0"
                Background="#E60A0E19"
                BorderBrush="#38BDF8"
                BorderThickness="1"
                CornerRadius="18"
                Padding="16 8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚡" Margin="0 0 8 0" Foreground="#00F5A0" FontSize="14"/>
                <TextBlock Text="TeachMe AI — Arrastra con el ratón para recortar el área de interés • "
                           Foreground="#F1F5F9"
                           FontWeight="SemiBold"
                           FontSize="13" />
                <Border Background="#33FFFFFF" CornerRadius="4" Padding="4 1" Margin="4 0">
                    <TextBlock Text="Esc" Foreground="#38BDF8" FontSize="11" FontWeight="Bold"/>
                </Border>
                <TextBlock Text=" cancelar" Foreground="#94A3B8" FontSize="13"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
