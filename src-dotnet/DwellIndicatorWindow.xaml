<Window x:Class="TeachMeAI.DwellIndicatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TeachMe AI Dwell Radar"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Width="180"
        Height="54"
        IsHitTestVisible="False">
    <Border Background="#E60A0E19"
            BorderBrush="#00F5A0"
            BorderThickness="1"
            CornerRadius="10"
            Padding="8 6"
            Margin="4">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" Color="#00F5A0" Opacity="0.4" ShadowDepth="0"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Spinner / Radar Icon -->
            <TextBlock Grid.Column="0" Text="⏳" Foreground="#00F5A0" FontSize="16" VerticalAlignment="Center" Margin="0 0 8 0"/>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="StatusText" Text="Analizando..." Foreground="#FFFFFF" FontWeight="SemiBold" FontSize="11"/>
                    <TextBlock x:Name="TimeText" Text=" (3.0s)" Foreground="#00F5A0" FontSize="11" Margin="4 0 0 0"/>
                </StackPanel>
                <TextBlock Text="O pulsa [Espacio] para abrir" Foreground="#94A3B8" FontSize="9" Margin="0 2 0 0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
